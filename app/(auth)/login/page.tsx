import { RoutePlaceholder } from "@/components/shared/route-placeholder";

export default function LoginPlaceholderPage() {
  return (
    <RoutePlaceholder
      title="Login"
      description="Placeholder route for the future authentication entry point. No form logic, validation, or API integration is added yet."
    />
  );
}